@model IEnumerable<CookBookAdoWeb.Models.TypeOfDish>

@{
    ViewBag.Title = "Типы блюда";
}

<h2>Типы блюда</h2>

<p>
    @Html.ActionLink("Добавить новый тип", "Create")
</p>
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.IdType)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.NameType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        @*<td>
            @Html.DisplayFor(modelItem => item.IdType)
        </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.NameType)
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id = item.IdType}) |
            @Html.ActionLink("Детали", "Details", new { id = item.IdType }) |
            @Html.ActionLink("Удалить", "Delete", new { id = item.IdType })
        </td>
    </tr>
}

</table>
